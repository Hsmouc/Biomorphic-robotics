   if(servo_num[1]==2)
    {
        if(time_now[1]!=0)
        { 
            if(time_mark[1])       
            {
                time_mark[1]=0;
                servo_time[1]=servo_time[1]+1;
            }
          
            if(servo_time[1]>=time_now[1])
            {
                servo_time[1]=0;
                if(servo_position_now[1]<position_now[1])
                {
                    servo_position_now[1]=servo_position_now[1]+1;
    
                }
                else if(servo_position_now[1]>position_now[1])
                {
                    servo_position_now[1]=servo_position_now[1]-1;
              
                }
                else
                {
                    servo_position_now[1]=position_now[1];
                    
                } 
            }
        }
        else
        {
               servo_position_now[1]=position_now[1];
               servo_time[1]=0;
        }
    }
    if(servo_num[2]==3)
    {
        if(time_now[2]!=0)
        { 
            if(time_mark[2])       
            {
                time_mark[2]=0;
                servo_time[2]=servo_time[2]+1;
            }
          
            if(servo_time[2]>=time_now[2])
            {
                servo_time[2]=0;
                if(servo_position_now[2]<position_now[2])
                {
                    servo_position_now[2]=servo_position_now[2]+1;
    
                }
                else if(servo_position_now[2]>position_now[2])
                {
                    servo_position_now[2]=servo_position_now[2]-1;
              
                }
                else
                {
                    servo_position_now[2]=position_now[2];
                    
                } 
            }
        }
        else
        {
               servo_position_now[2]=position_now[2];
               servo_time[2]=0;
        }
    }
    if(servo_num[3]==4)
    {
        if(time_now[3]!=0)
        { 
            if(time_mark[3])       
            {
                time_mark[3]=0;
                servo_time[3]=servo_time[3]+1;
            }
          
            if(servo_time[3]>=time_now[3])
            {
                servo_time[3]=0;
                if(servo_position_now[3]<position_now[3])
                {
                    servo_position_now[3]=servo_position_now[3]+1;
    
                }
                else if(servo_position_now[3]>position_now[3])
                {
                    servo_position_now[3]=servo_position_now[3]-1;
              
                }
                else
                {
                    servo_position_now[3]=position_now[3];
                    
                } 
            }
        }
        else
        {
               servo_position_now[3]=position_now[3];
               servo_time[3]=0;
        }
    }
    if(servo_num[4]==5)
    {
        if(time_now[4]!=0)
        { 
            if(time_mark[4])       
            {
                time_mark[4]=0;
                servo_time[4]=servo_time[0]+1;
            }
          
            if(servo_time[0]>=time_now[4])
            {
                servo_time[4]=0;
                if(servo_position_now[4]<position_now[4])
                {
                    servo_position_now[4]=servo_position_now[4]+1;
    
                }
                else if(servo_position_now[4]>position_now[4])
                {
                    servo_position_now[4]=servo_position_now[4]-1;
              
                }
                else
                {
                    servo_position_now[4]=position_now[4];
                    
                } 
            }
        }
        else
        {
               servo_position_now[4]=position_now[4];
               servo_time[4]=0;
        }
    }
    if(servo_num[5]==6)
    {
        if(time_now[5]!=0)
        { 
            if(time_mark[5])       
            {
                time_mark[5]=0;
                servo_time[5]=servo_time[5]+1;
            }
          
            if(servo_time[5]>=time_now[5])
            {
                servo_time[5]=0;
                if(servo_position_now[5]<position_now[5])
                {
                    servo_position_now[5]=servo_position_now[5]+1;
    
                }
                else if(servo_position_now[5]>position_now[5])
                {
                    servo_position_now[5]=servo_position_now[5]-1;
              
                }
                else
                {
                    servo_position_now[5]=position_now[5];
                    
                } 
            }
        }
        else
        {
               servo_position_now[5]=position_now[5];
               servo_time[5]=0;
        }
    }
    if(servo_num[6]==7)
    {
        if(time_now[6]!=0)
        { 
            if(time_mark[6])       
            {
                time_mark[6]=0;
                servo_time[6]=servo_time[6]+1;
            }
          
            if(servo_time[6]>=time_now[6])
            {
                servo_time[6]=0;
                if(servo_position_now[6]<position_now[6])
                {
                    servo_position_now[6]=servo_position_now[6]+1;
    
                }
                else if(servo_position_now[6]>position_now[6])
                {
                    servo_position_now[6]=servo_position_now[6]-1;
              
                }
                else
                {
                    servo_position_now[6]=position_now[6];
                    
                } 
            }
        }
        else
        {
               servo_position_now[0]=position_now[0];
               servo_time[0]=0;
        }
    }
    if(servo_num[7]==8)
    {
        if(time_now[7]!=0)
        { 
            if(time_mark[7])       
            {
                time_mark[7]=0;
                servo_time[7]=servo_time[7]+1;
            }
          
            if(servo_time[7]>=time_now[7])
            {
                servo_time[7]=0;
                if(servo_position_now[7]<position_now[7])
                {
                    servo_position_now[7]=servo_position_now[7]+1;
    
                }
                else if(servo_position_now[7]>position_now[7])
                {
                    servo_position_now[7]=servo_position_now[7]-1;
              
                }
                else
                {
                    servo_position_now[7]=position_now[7];
                    
                } 
            }
        }
        else
        {
               servo_position_now[7]=position_now[7];
               servo_time[7]=0;
        }
    }
    if(servo_num[8]==9)
    {
        if(time_now[8]!=0)
        { 
            if(time_mark[8])       
            {
                time_mark[8]=0;
                servo_time[8]=servo_time[8]+1;
            }
          
            if(servo_time[8]>=time_now[8])
            {
                servo_time[8]=0;
                if(servo_position_now[8]<position_now[8])
                {
                    servo_position_now[8]=servo_position_now[8]+1;
    
                }
                else if(servo_position_now[8]>position_now[8])
                {
                    servo_position_now[8]=servo_position_now[8]-1;
              
                }
                else
                {
                    servo_position_now[8]=position_now[8];
                    
                } 
            }
        }
        else
        {
               servo_position_now[8]=position_now[8];
               servo_time[8]=0;
        }
    }
    if(servo_num[9]==10)
    {
        if(time_now[9]!=0)
        { 
            if(time_mark[9])       
            {
                time_mark[9]=0;
                servo_time[9]=servo_time[9]+1;
            }
          
            if(servo_time[9]>=time_now[9])
            {
                servo_time[9]=0;
                if(servo_position_now[9]<position_now[9])
                {
                    servo_position_now[9]=servo_position_now[9]+1;
    
                }
                else if(servo_position_now[9]>position_now[9])
                {
                    servo_position_now[9]=servo_position_now[9]-1;
              
                }
                else
                {
                    servo_position_now[9]=position_now[9];
                    
                } 
            }
        }
        else
        {
               servo_position_now[9]=position_now[9];
               servo_time[9]=0;
        }
    }
    if(servo_num[10]==11)
    {
        if(time_now[10]!=0)
        { 
            if(time_mark[10])       
            {
                time_mark[10]=0;
                servo_time[10]=servo_time[10]+1;
            }
          
            if(servo_time[10]>=time_now[10])
            {
                servo_time[10]=0;
                if(servo_position_now[10]<position_now[10])
                {
                    servo_position_now[10]=servo_position_now[10]+1;
    
                }
                else if(servo_position_now[10]>position_now[10])
                {
                    servo_position_now[10]=servo_position_now[10]-1;
              
                }
                else
                {
                    servo_position_now[10]=position_now[10];
                    
                } 
            }
        }
        else
        {
               servo_position_now[10]=position_now[10];
               servo_time[10]=0;
        }
    }
    if(servo_num[11]==12)
    {
        if(time_now[11]!=0)
        { 
            if(time_mark[11])       
            {
                time_mark[11]=0;
                servo_time[11]=servo_time[11]+1;
            }
          
            if(servo_time[11]>=time_now[11])
            {
                servo_time[11]=0;
                if(servo_position_now[11]<position_now[11])
                {
                    servo_position_now[11]=servo_position_now[11]+1;
    
                }
                else if(servo_position_now[11]>position_now[11])
                {
                    servo_position_now[11]=servo_position_now[11]-1;
              
                }
                else
                {
                    servo_position_now[11]=position_now[11];
                    
                } 
            }
        }
        else
        {
               servo_position_now[11]=position_now[11];
               servo_time[11]=0;
        }
    }
    if(servo_num[12]==13)
    {
        if(time_now[12]!=0)
        { 
            if(time_mark[12])       
            {
                time_mark[12]=0;
                servo_time[12]=servo_time[12]+1;
            }
          
            if(servo_time[12]>=time_now[12])
            {
                servo_time[12]=0;
                if(servo_position_now[12]<position_now[12])
                {
                    servo_position_now[12]=servo_position_now[12]+1;
    
                }
                else if(servo_position_now[12]>position_now[12])
                {
                    servo_position_now[12]=servo_position_now[12]-1;
              
                }
                else
                {
                    servo_position_now[12]=position_now[12];
                    
                } 
            }
        }
        else
        {
               servo_position_now[12]=position_now[12];
               servo_time[12]=0;
        }
    }
    if(servo_num[13]==14)
    {
        if(time_now[13]!=0)
        { 
            if(time_mark[13])       
            {
                time_mark[13]=0;
                servo_time[13]=servo_time[13]+1;
            }
          
            if(servo_time[13]>=time_now[13])
            {
                servo_time[13]=0;
                if(servo_position_now[13]<position_now[0])
                {
                    servo_position_now[0]=servo_position_now[0]+1;
    
                }
                else if(angle_temp>angle_set)
                {
                    servo_position_now[0]=servo_position_now[0]-1;
              
                }
                else
                {
                    servo_position_now[0]=position_now[0];
                    
                } 
            }
        }
        else
        {
               servo_position_now[0]=position_now[0];
               servo_time[0]=0;
        }
    }
    if(servo_num[14]==15)
    {
        if(time_now[0]!=0)
        { 
            if(time_mark[0])       
            {
                time_mark[0]=0;
                servo_time[0]=servo_time[0]+1;
            }
          
            if(servo_time[0]>=time_now[0])
            {
                servo_time[0]=0;
                if(servo_position_now[0]<position_now[0])
                {
                    servo_position_now[0]=servo_position_now[0]+1;
    
                }
                else if(angle_temp>angle_set)
                {
                    servo_position_now[0]=servo_position_now[0]-1;
              
                }
                else
                {
                    servo_position_now[0]=position_now[0];
                    
                } 
            }
        }
        else
        {
               servo_position_now[0]=position_now[0];
               servo_time[0]=0;
        }
    }
    if(servo_num[15]==16)
    {
        if(time_now[0]!=0)
        { 
            if(time_mark[0])       
            {
                time_mark[0]=0;
                servo_time[0]=servo_time[0]+1;
            }
          
            if(servo_time[0]>=time_now[0])
            {
                servo_time[0]=0;
                if(servo_position_now[0]<position_now[0])
                {
                    servo_position_now[0]=servo_position_now[0]+1;
    
                }
                else if(angle_temp>angle_set)
                {
                    servo_position_now[0]=servo_position_now[0]-1;
              
                }
                else
                {
                    servo_position_now[0]=position_now[0];
                    
                } 
            }
        }
        else
        {
               servo_position_now[0]=position_now[0];
               servo_time[0]=0;
        }
    }